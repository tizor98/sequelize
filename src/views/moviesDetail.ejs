<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= movie.title %> </h1>    
    <br>
    <% if (movie.genres) { %>
      <p>GENRE: <%= movie.genres.name %> </p>
    <% } %>
    <p>RATING: <%= movie.rating %> </p>
    <p>AWARDS: <%= movie.awards %> </p>
    <p>LENGTH: <%= movie.length + ' min'%> </p>
    <p>RELEASE DATE: <%= movie.release_date %> </p>
    <br>
   <hr>
   <br>
   
    <form action="/movies/edit/<%= movie.id %>" method="get">
      <button class="botonAgregar" type="submit">Editar</button>
   </form>
   <br>

   <form action="/movies/delete/<%= movie.id %>" method="get">
      <button class="botonBorrar" type="submit">Eliminar</button>
   </form>
   <br>

   <form action="/movies" method="get">
      <button class="botonVolver" type="submit">Lista de películas</button>
   </form>

</body>
</html>